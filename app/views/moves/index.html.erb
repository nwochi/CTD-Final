<p id="notice"><%= notice %></p>

<h1>Moves in My Phrase</h1>

<table>
    <thead>
        <tr>
            <th>List of Moves</th>
            <th colspan="3"></th>
        </tr>
    </thead>
        
    <tbody>
        <% @phrases.moves.each do |move| %>
            <tr>
                <td><%= phrase.move %></td>
                <td><%= link_to 'Create', create_phrase_path(phrase) %> |</td>
                <td><%= link_to 'Show', phrase.moves %> |</td>
                <td><%= link_to 'Edit', edit_phrase.moves_path(phrase) %> |</td>
                <td><%= link_to 'Delete', phrase.moves.delete(@move), method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
        <% end %>
        
    </tbody>
    
</table>

<br>

<div>
    <%= link_to 'Home', root_path %>
</div>