<p id="notice"><%= notice %></p>

<h1>My Phrases</h1>

<table>
    <thead>
        <tr>
            <th>Phrase Name</th>
            <th colspan="3"></th>
        </tr>
    </thead>
        
    <tbody>
        <% @phrases.each do |phrase| %>
            <tr>
                <td><%= phrase.name %></td>
                <!--<td>< %= link_to 'Create', create_phrase_path(phrase) %> |</td>-->
                <td><%= link_to 'Show', phrase %> |</td>
                <td><%= link_to 'Edit', edit_phrase_path(phrase) %> |</td>
                <td><%= link_to 'Destroy', phrase, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
        <% end %>
        
    </tbody>
    
</table>

<br>
<div>
    <%= link_to 'Home', root_path %> |
    <%= link_to 'New Phrase', new_phrase_path %>
</div>

<!--< %= link_to 'New Phrase', new_phrase_path %>-->
<!--    < % @phrases.each do |phrase| %>-->
<!--    <p> < %= link_to phrase.name, phrase_path(phrase) %></p>-->
    <!--...name, "/phrases/#{phrase.id}"-->
<!--    < % end %>-->