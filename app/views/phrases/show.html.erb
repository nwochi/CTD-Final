<!--<h1>< %= @phrase.name %></h1>-->
<!DOCTYPE HTML>
<html lang="en">
    <head>
        <title>Moves in Your Phrase</title>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <!--<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        
        <!--<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>-->
    </head>
    
    <body>
        <p id="notice"><%= notice %></p>
        <div class="jumbotron jumbotron-fluid bg-dark text-white">
            <div class="container text-lg-center">
                <h1 class="display-1"><strong>Name:</strong><%= @phrase.name %></h1>
            </div>
        </div>
        
        <div>
            <h2>Moves in Your Phrase</h2>
                <% @phrase.moves.order(:position).each do |t| %>
                    <% if !t.position.nil? %>
                        <%= t.position.to_s + ": " + t.name %> <br>
                    <% end %>
                <% end %>
        </div>
            <div class="bg-image">
                <img src="https://assets.rockettes.com/wp-content/uploads/2015/07/first-ballet-class-article-1-1024x672.jpg" class="w-100" alt="Getting Started" />
                <div class="mask" style="background-color: rgba(0, 0, 0, 0.6)">
                    <div class="d-flex justify-content-center align-items-center h-100">
                        <!--<p>text</p>-->
                    </div>
                </div>
            </div>
        </div>


<!--<div id="moves">-->

<!--<div id="moves">-->
    <!--<h2>Moves in Your Phrase</h2>-->
    <!--< % @phrases.moves.each do |move| %>-->
    <!--    <strong>< %= move.name %></strong>-->
    <!--    < %= move.position %>-->
        
    <!--< % @phrase.moves.order(:position).each do |t| %>-->
    <!--    < % if !t.position.nil? %>-->
    <!--        < %= t.position.to_s + ": " + t.name %> <br>-->
    <!--    < % end %>-->
    <!--< % end %>-->
    <!--< % end %>-->
<!--</div>-->

<div>
    <nav class="navbar-expand-lg navbar-dark bg-dark fixed-bottom">
    <%= link_to 'Edit Phrase Name', edit_phrase_path(@phrase) %> |
    <%= link_to 'Edit Moves', edit_moves_path(@phrase) %> |
    <%= link_to 'Back to My Phrases', phrases_path %> |
    <%= link_to 'Home', root_path %>
    <!--< %= link_to 'delete', @phrase, method: :delete %>-->
    </nav>
</div>

</body>
</html>